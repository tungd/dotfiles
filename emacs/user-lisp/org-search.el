;;; org-search.el --- Semantic indexing and search for Org mode files -*- lexical-binding: t; -*-

;; Using vector similarity search to index and search Org mode files

;;; Commentary:
;;
;; Provides `org-semantic' command to perform similarity search on org-mode files
;; Provides `org-semantic-tracking-mode' to automatically track visited org-mode files, and re-index them when updated
;; Customization variables let you pick the program and its args.
;;
;;; Code:


(provide 'org-semantic)

;;; org-search.el ends here
