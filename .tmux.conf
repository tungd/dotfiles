# Keep terminal control sequences contained inside tmux panes.
# vterm-agent should use structured side channels for Codex hook events
# instead of relying on OSC passthrough to the outer terminal.
set -g allow-passthrough off
